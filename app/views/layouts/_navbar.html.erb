<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-default-collapse" aria-expanded="false">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="#"><%= Settings.framework_name %></a>
    </div>

    <div class="collapse navbar-collapse" id="navbar-default-collapse">
      <ul class="nav navbar-nav">
        <li><%= link_to "Home", root_path %></li>
      </ul>
      <ul class="nav navbar-nav navbar-right">
        <% if current_user && current_user.admin? %>
          <li><%= link_to "Admin", rails_admin_path %></li>
        <% end %>
        <% if current_user %>
          <li><%= link_to "Logout (#{current_user.email})", logout_path %></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>